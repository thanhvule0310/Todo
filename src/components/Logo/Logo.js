import React from 'react';
import styled from 'styled-components';

const LogoStyled = styled.div`
  text-align: center;
  font-size: 2rem;
  display: inline-block;
  text-transform: uppercase;
  font-weight: bold;
  color: var(--color-main);
  cursor: pointer;
`;

const Logo = props => <LogoStyled> TODO </LogoStyled>;

export default Logo;
